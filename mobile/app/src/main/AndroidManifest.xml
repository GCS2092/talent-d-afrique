<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TalentDAfriqueApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="Talent d'Afrique"
        android:theme="@style/Theme.TalentDAfrique"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic="true" : nécessaire uniquement en dev pour parler à
          http://10.0.2.2:8000 (ton backend local en HTTP). À retirer une fois
          que le backend est en HTTPS (Supabase / prod).
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TalentDAfrique">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
